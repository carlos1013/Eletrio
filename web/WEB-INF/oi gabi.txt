//https://stackoverflow.com/questions/27202591/how-to-fix-on-delete-cascade-on-update-cascade-using-derby-embeddeddriver

CREATE TABLE CATEGORIA(
    ID          INT          NOT NULL	PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
    DESCRICAO   VARCHAR(300) NOT NULL);

CREATE TABLE PRODUTO(
    ID             INT            NOT NULL	PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
    ID_CATEGORIA   INT            NOT NULL       REFERENCES CATEGORIA(ID),
    NOME           VARCHAR(100)   NOT NULL,
    DESCRICAO      VARCHAR(200)   NOT NULL,
    VALOR          DECIMAL(10,2)  NOT NULL);

CREATE TABLE CLIENTES(
    ID             INT            NOT NULL	PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
    NOME           VARCHAR(250)   NOT NULL,
    ENDERECO       VARCHAR(250)   NOT NULL,
    REFERENCIA     VARCHAR(250),
    BAIRRO         VARCHAR(50)    NOT NULL,
    CIDADE         VARCHAR(70)    NOT NULL,
    CEP            CHAR(8)        NOT NULL,
    ESTADO         VARCHAR(50)    NOT NULL,
    CPF            CHAR(11)       NOT NULL,
    IDENTIDADE     VARCHAR(20)    NOT NULL,
    FIXO           VARCHAR(11),
    CELULAR        VARCHAR(11),
    CARTAO         VARCHAR(24)    NOT NULL,
    BANDEIRA       VARCHAR(14)    NOT NULL);

CREATE TABLE COMPRAS(
    ID             INT      NOT NULL	PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
    ID_CLIENTE     INT      NOT NULL,
    ID_PRODUTO     INT      NOT NULL,
    FOREIGN KEY (ID_CLIENTE) REFERENCES CLIENTES(ID),
    FOREIGN KEY (ID_PRODUTO) REFERENCES PRODUTO(ID));

CREATE TABLE ADMINISTRADOR(
    ID             INT          NOT NULL	PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
    LOGIN          VARCHAR(20)  NOT NULL,
    SENHA          BIGINT   NOT NULL);